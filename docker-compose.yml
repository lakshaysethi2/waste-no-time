services:
  web:
    build: .
    container_name: waste-no-time
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    ports:
      - "8000:8000"
    env_file:
      - .env.local
    restart: unless-stopped
